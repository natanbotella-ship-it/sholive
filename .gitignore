# Next.js
/.next/
/out/

# Dependencies
/node_modules/

# Env — jamais commité, contient les clés Stripe/Supabase
.env
.env.local
.env*.local

# Build
/build
*.tsbuildinfo
next-env.d.ts
/coverage

# Misc
.DS_Store
*.pem
npm-debug.log*
.vercel

# Claude Code — settings locaux, pas partagés
.claude/settings.local.json

# Stripe CLI — logs/pid du "stripe listen" lance en local pour le Bloc 19
stripe-listen.log
stripe-listen.err.log
stripe-listen.pid
